# Telegram API credentials
api_id=your_api_id_here
api_hash=your_api_hash_here

# Database
DATABASE_URL=sqlite:///./tbot.db

# DEPRECATED: Use web UI to manage channels instead
# These environment variables are kept for backward compatibility
# Run `python migrate_channels_to_db.py` to migrate to database
target_channel_id=-2198949181
test_channel_id=-2907147155

# Server settings
HOST=0.0.0.0
PORT=8000
